# Дана матрица а из трех столбцов, # описывающая набор прямых (см. задание 3). # Нобходимо пересечь каждую прямую с каждой. # Подробней так: каждое пересечение прямой из строки i # с прямой из строки j дает две координаты пересечения x и y. # Функция должна вернуть две матрицы x и y. # Первая содержит x координату этого пересечения в строчке i и столбце j, # вторая содержит, соответственно, y координату. # Не обрабатывайте отдельно случаи параллельных прямых, # пусть для параллельных прямых получается произвольный ответ, # скорее всего, у вас это будет NaN из-за деления 0 на 0.# Напомню формулу пересечения прямых a1x+b1y+c1 = 0 и a2x+b2y+c2 = 0:# Δ = det([a1 b1;a2 b2])# Δx = det([-c1 b1;-c2 b2])# Δy = det([a1 -c1;a2 -c2])# x = Δx / Δ# y = Δy / Δfunction [f1, f2] = all_lines_intersections(X)    a_ = X(:, 1)    b_ = X(:, 2)    d = all2dets([a_ b_])    c_ = -X(:, 3)    dx = all2dets([c_ b_])    dy = all2dets([a_ c_])    x = dx ./d    y = dy ./d    f1 = x    f2 = y endfunction